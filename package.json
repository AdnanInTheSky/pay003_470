{
  "name": "dhaka-market-paystation",
  "version": "1.0.0",
  "description": "E-commerce with PayStation BD + MongoDB. Vanilla JS/HTML + Vercel serverless.",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "mongodb": "^6.8.0"
  },
  "devDependencies": {
    "vercel": "^34.0.0"
  },
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  }
}